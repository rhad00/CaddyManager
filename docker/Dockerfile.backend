FROM node:20-alpine

WORKDIR /app

# Install dependencies first (better layer caching)
COPY ../backend/package*.json ./
RUN npm ci

# Copy source
COPY ../backend .

# Build TypeScript
RUN npm run build

EXPOSE 3000

CMD ["npm", "run", "dev"]
