# ============================================
# CaddyManager Backend Environment Variables
# ============================================

# Application Configuration
# --------------------------
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Database Configuration
# ----------------------
# Database type: 'sqlite' or 'postgres'
DB_TYPE=sqlite

# SQLite Configuration (when DB_TYPE=sqlite)
SQLITE_PATH=./database.sqlite

# PostgreSQL Configuration (when DB_TYPE=postgres)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=caddymanager
# DB_USER=caddyuser
# DB_PASSWORD=your_secure_password
# DB_SSL=false
# DB_URL=postgresql://caddyuser:your_secure_password@localhost:5432/caddymanager

# Authentication Configuration
# ----------------------------
# REQUIRED: Use a long random string for production
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRES_IN=24h

# Initial Admin User (created on first startup if no users exist)
# ----------------------------------------------------------------
ADMIN_EMAIL=admin@caddymanager.local
ADMIN_PASSWORD=changeme123

# Caddy Configuration
# -------------------
# Use 'http://caddy:2019' in Docker, 'http://localhost:2019' for local development
CADDY_API_URL=http://caddy:2019

# Optional: Cloudflare API token for DNS-01 challenges
# If you use Cloudflare DNS challenges with Caddy, set this token so the
# backend can expose the Cloudflare option in the frontend and perform
# optional TLS checks. In Docker, set this in your compose file or in an
# env file used with `--env-file` for docker-compose.
# CF_API_TOKEN=your_cloudflare_api_token_here
